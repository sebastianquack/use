<p id="notice"><%= params[:notice] %></p>

<h1>New player to player transaction</h1>

<%= form_for(@transaction, :url => {:action => "create_public"}) do |f| %>
  <% if @transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
      <% @transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= f.hidden_field :transaction_type_id, :value => "0" %>
  
  <p>
    <%= f.select :seller_id, User.where("role = 'player'").order("name").collect {|u| [u.name, u.id]} %>
    VERKAUFT AN 
    <%= f.select :buyer_id, User.where("role = 'player'").order("name").collect {|u| [u.name, u.id]} %>
  </p>

  <p>
    <%= f.number_field :amount, value: 0 %>
    ANTEILE DER UTOPIE
    <%= f.select :stock_id, Stock.where('active = true').order("name").collect {|s| [s.name + " ("+s.symbol+")", s.id]} %>
 </p>

  <p>
    ZUM PREIS VON JE <%= f.number_field :price, value: 0 %>
  </p>
  
  <p>DAS MACHT INSGESAMT: <span class="total">0</span></p>

  <div class="actions">
    <%= f.submit 'TRANSAKTION DURCHFÜHREN' %>
  </div>
<% end %>
