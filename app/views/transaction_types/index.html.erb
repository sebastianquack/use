<h1>Listing transaction_types</h1>

<table>
  <thead>
    <tr>
      <th>Available</th>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @transaction_types.each do |transaction_type| %>
      <tr>
        <td><%= transaction_type.available %></td>
        <td><%= transaction_type.name %></td>
        <td><%= link_to 'Show', transaction_type %></td>
        <td><%= link_to 'Edit', edit_transaction_type_path(transaction_type) %></td>
        <td><%= link_to 'Destroy', transaction_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Transaction type', new_transaction_type_path %>
