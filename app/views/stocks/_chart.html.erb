
<table>
    <tr>
        <th>time</th>
        <th>amount</th>
        <th>price</th>
    </tr>
<% stock.transactions.each do |t| %>
    <tr>
        <td><%= t.created_at %></td>
        <td><%= t.amount %></td>
        <td><%= t.price %></td>
    </tr>

<% end %>
</table>
