<%= form_for(@market_session) do |f| %>
  <% if @market_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@market_session.errors.count, "error") %> prohibited this market_session from being saved:</h2>

      <ul>
      <% @market_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :duration %><br>
    <%= f.number_field :duration %>
  </div>
  <div class="field">
    <%= f.label :max_survivors %><br>
    <%= f.number_field :max_survivors %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
